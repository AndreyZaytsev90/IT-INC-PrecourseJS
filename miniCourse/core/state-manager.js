const _state = {
    points: {
        google: 12,
        players: [10, 11]
    }
}

export const getGooglePoints = () => _state.points.google

export const getPlayersPoints = (playerNumber) => {
    
}